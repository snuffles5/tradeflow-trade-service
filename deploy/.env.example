# Application container image (override if using a different tag)
APP_IMAGE=ghcr.io/your-org/tradeflow-trade-service:latest

# Public HTTP port to expose the application on the host
APP_PORT=9090

# Database settings
DB_NAME=tradeflow
DB_USER=admin@snuffles.com
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=default
DB_PORT=3306

# Optional Spring overrides
SPRING_PROFILES_ACTIVE=prod
